name: widgets_test_screen
description: A new Flutter project.


publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  just_audio: ^0.9.30
  liquid_pull_to_refresh:
  file_picker:
  path:
  toastification:
  tap_to_expand:
  reaction_askany:
  qr_flutter:
  flutter_staggered_grid_view:
  translator:


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0





flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/mp3s/
    - assets/gifs/
    - assets/product_images/
    - assets/env/
